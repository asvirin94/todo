import styled from "@emotion/styled";
import { PALETTE } from "../../../consts/palette";

export const Title = styled.h1`
    font-size: 28px;
    color: ${PALETTE.black};
    margin-bottom: 50px;

    span {
        color: ${PALETTE.gray};
    }
`