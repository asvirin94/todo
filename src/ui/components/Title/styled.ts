import styled from "@emotion/styled";
import { PALETTE } from "../../../consts/palette";

export const Title = styled.h1`
    font-size: 43px;
    color: ${PALETTE.black};

    span {
        color: ${PALETTE.gray};
    }
`